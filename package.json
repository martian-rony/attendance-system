{
  "name": "attendance-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Online Attendance Management System - Render single-service deploy (backend serves built frontend)",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/src/server.js"
  }
}
