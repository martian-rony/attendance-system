// Vitest setup. Helpers are pure functions tested in the node environment,
// so no DOM/jest-dom setup is required here. (Frontend component tests would
// add @testing-library/react + jsdom and import '@testing-library/jest-dom'.)
export {};
